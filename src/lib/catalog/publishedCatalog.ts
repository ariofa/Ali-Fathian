import type { Product } from './types';

/**
 * Public catalogue records are intentionally empty until a real brand/product
 * is authorised for publication. Never add demo or invented products here.
 */
export const PUBLISHED_CATALOG_PRODUCTS: Product[] = [];
