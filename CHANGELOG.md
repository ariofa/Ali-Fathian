# ایران‌بیم‌هاب — دفترچه تغییرات (CHANGELOG)

هر نسخه منتشرشده با شماره فایل ZIP خودش اینجا ثبت می‌شود تا رهگیری نسخه‌ها ساده باشد.
نام فایل‌ها: `IranBIMhub_MM.DD_NN.zip`

---

## نسخه ۱۴۰۵/۰۵/۱۶_۰۵ — اصلاحات صفحهٔ تماس، نظرات یکپارچه و پنهان‌سازی هوشمند لینک‌ها
**این نسخه = فایل `IranBIMhub_05.16_05.zip`**

### تغییر ۱ — عنوان صفحهٔ تماس: «پشتیبانی فنی و مشاوره»
- «تماس با ما و دپارتمان‌های پشتیبانی» → «پشتیبانی فنی و مشاوره»؛ هیچ اثری از واژه‌های «دپارتمان» و «بازرگانی» در صفحه نیست (کارت تیره → «اطلاعات تماس ایران‌بیم‌هاب»، دپارتمان دوم تیکت → «پشتیبانی عمومی»)
- جملهٔ بیزنسی: «هر پرسشی که امروز مطرح می‌کنید، تصمیمی مطمئن‌تر برای فردای پروژهٔ شماست…» (+ معادل انگلیسی)

### تغییر ۲ — مسیردهی تولیدکنندگان در متن هیرو
- جمله: «مهندسان و نمایندگان کارخانجات، تولیدکنندگان و صاحبان برند می‌توانند درخواست‌های ارزیابی، کنترل کیفی، تبدیل فایل و همکاری‌های سازمانی را از طریق **بخش محور تولیدکنندگان** ارسال کنند» — با لینک کلیک‌پذیر به صفحهٔ «برای تولیدکنندگان»

### تغییر ۳ — افزودن تلفن به کارت تماس
- ردیف تلفن (آیکن + لینک `tel:`) به کارت تیره اضافه شد؛ مقدار از کانفیگ ادمین (`footer.phone`)، پیش‌فرض: ۰۲۱-۸۸۸۸۷۷۶۷؛ در فوتر هم همین مقدار با لینک tel نمایش داده می‌شود

### تغییر ۴ و ۵ — بازگشت «نظرات متخصصان» به صفحهٔ اول + کامپوننت اشتراکی
- فایل جدید `src/components/ExpertInsightsSection.tsx`: **یک کد واحد** که در `HomeView` و `ForDesignersView` رندر می‌شود
- منبع واحد: `siteConfig.expertInsights` از پنل ادمین (نام/نقش/متن FA+EN + پرچم انتشار) — با انتشار اولین نظر واقعی، در **هر دو صفحه هم‌زمان** جایگزین می‌شود
- تا آن زمان: نقل‌قول‌های اجتماعیِ شفاف و بدون نسبت‌دهی فردی («از گفت‌وگوهای اولیه با جامعهٔ طراحی»)
- عنوان بخش (FA/EN): «نظرات و دیدگاه‌های متخصصان حوزه مدل‌سازی اطلاعاتی ساختمان (BIM)»
- تأیید QA: محتوای دو صفحه دقیقاً یکسان (innerText برابر)

### تغییر ۶ — شبکه‌های اجتماعی: فقط در صورت وجود لینک نمایش داده می‌شوند
- `SocialIconsRow`، کارت «ارتباط مستقیم»، کارت «دنبال کنید» و فوتر: هر کانال فقط وقتی رندر می‌شود که ادمین URL آن را درج کرده باشد؛ اگر همهٔ لینک‌های یک کارت خالی شود، خودِ کارت هم پنهان است
- fallbackهای هاردکد از همهٔ نقاط حذف شد؛ تست QA: با خالی‌شدن ۴ کانال، دقیقاً همان ۴ آیکن/کارت ناپدید و بقیه باقی ماندند

### تغییر ۷ (فنی) — پاک‌سازی fallbackهای ساختگیٔ فوتر + پایداری ذخیرهٔ ادمین
- فوتر قبلاً تلفن و آدرس **جعلیِ متفاوت** (تهران، پارک فناوری پردیس) را در نبود کانفیگ نمایش می‌داد — حالا سطرهای ایمیل/تلفن/آدرس فقط با مقدار واقعی کانفیگ رندر می‌شوند
- `config.json` (فایل ذخیرهٔ تنظیمات ادمین) به watch-ignore محیط dev افزوده شد تا ذخیرهٔ تنظیمات، صفحه را رفرش نکند
- نکتهٔ کشف‌شده: فایل تنظیمات سایت در ریشهٔ پروژه `config.json` است (نه `data/`)؛ مقدار تلفن در همان فایل ذخیره و تحویل داده می‌شود

---

## نسخه ۱۴۰۵/۰۵/۱۶_۰۴ — بستهٔ ۱۱ موردی: اطلاعات تماس یکپارچه، تیکت واقعی، Breadcrumb سراسری
**این نسخه = فایل `IranBIMhub_05.16_04.zip`**

این بسته ۴ مورد معلّق گزارش‌شده در نسخهٔ ۰۳ را (با تأیید مالک محصول) اجرا و بقیهٔ درخواست‌های بستهٔ ۱۱ موردی را تکمیل کرد:

### تغییر ۱ — حذف پوشه‌های پروژهٔ بذر ساختگی در داشبورد مدل‌ساز
- `ModelerDashboard.tsx`: پوشه‌های پیش‌فرض («هتل شیراز»...) حذف؛ کاربر تازه‌ثبت‌نام با `EmptyState` صادقانه و دعوت به ساخت اولین پوشه مواجه می‌شود (ابزار + تب پروژه‌ها)

### تغییر ۲ — حذف کلکسیون‌های بذر ساختگی در داشبورد مدل‌ساز
- پیش‌فرض `collections` خالی شد + `EmptyState` مناسب

### تغییر ۳ — شمارش دانلود و سهمیهٔ روزانهٔ واقعی
- فایل جدید `src/lib/downloadHistory.ts`: کلید `iranbimhub_dl_history`، سقف `DAILY_FREE_LIMIT = ۵`، مهاجرت خودکار دادهٔ قدیمی
- `App.tsx`: gatekeeper دانلود واقعی + ثبت نام سازنده در تاریخچه
- `ModelerDashboard`: نمایش «باقی‌ماندهٔ امروز» از دادهٔ واقعی (کاربر تازه = ۵/۵)؛ باگ نمایش دانلود مجدد با تاریخ ثابت ساختگی رفع شد

### تغییر ۴ — بازگرداندن و یکپارچه‌سازی بخش «نظرات» لندینگ با صفحهٔ معماران
- گرید نظرات در هر دو نقطه از یک منبع واحد تغذیه می‌شود: `siteConfig.expertInsights` (قابل مدیریت در پنل ادمین، با پرچم انتشار)
- تا انتشار اولین نظر واقعی، صفحهٔ معماران نقل‌قول‌های اجتماعیِ بدون نسبت‌دهی فردی نشان می‌دهد (بدون جعل هویت)
- ~۱۳۲ خط کد مردهٔ کنترل‌گر کاروسل قدیمی از `HomeView.tsx` حذف شد

### تغییر ۵ — کارت‌های آبجکت صفحهٔ «برای معماران» کلیک‌پذیر شدند
- کلیک روی کارت → صفحهٔ جزئیات آبجکت (`role=button`، اورلی هاور، توقف انتشار روی دکمه‌های داخلی)؛ سیم‌کشی در `App.tsx`

### تغییر ۶ — اطلاعات تماس و شبکه‌های اجتماعی: منبع واحد + قابل ویرایش در ادمین
- `server.ts`: فیلدهای `footer` شامل تلفن/ایمیل/آدرس + واتس‌اپ، آپارات، بله، یوتیوب، X و فیلدهای ویدیوی تولیدکنندگان در defaults
- `SiteConfigContext.tsx` با اینترفیس یکسان شد؛ `SocialLinks.tsx` → `SocialIconsRow` کاملاً config-driven با fallback امن
- همهٔ لینک‌های هاردکدِ صفحات (تماس، معرفی، برای تولیدکنندگان، مشاوره و...) به همان config متصل شدند
- پنل ادمین: فرم فوتر + ۹ ورودی URL شبکه‌های اجتماعی

### تغییر ۷ — کارت تماس: «بخش پشتیبانی ایران‌بیم‌هاب و دپارتمان بازرگانی»
- عنوان جایگزین «دبیرخانه مرکزی» شد؛ شماره تلفن و توضیحات اضافه حذف؛ ایمیل config-driven با `mailto` + ردیف شبکه‌های اجتماعی

### تغییر ۸ — سیستم تیکت واقعی سرتاسری
- عنوان: «ارسال تیکت به بخش‌های پشتیبانی»؛ بخش «پذیرش کارخانه» حذف (دو دپارتمان: فنی / عمومی و بازرگانی)
- `server.ts`: `POST /api/tickets` (honeypot، شماره پیگیری `IBH-YYYY-NNN`، ذخیره در `data/support-tickets.json`) + `GET /api/admin/tickets` + `PATCH /api/admin/tickets/:id`
- فرانت: ثبت واقعی async + نمایش شماره پیگیری در پیام موفقیت
- پنل ادمین: سینک تیکت‌های سرور، نمایش شماره پیگیری، پاسخ/ارجاع ↔ به‌روزرسانی وضعیت روی سرور

### تغییر ۹ — کارت‌های CTA مشاوره در صفحهٔ تماس
- دو کارت: «مشاورهٔ رایگان برند/تولیدکنندگان» (تیره → for-manufacturers) و «راهنمای معماران و مهندسان» (روشن → for-designers) با متن کوتاه

### تغییر ۱۰ — اسلات ویدیوی معرفی تولیدکنندگان در اسلاید سوم هیرو
- اسلات همیشه رندر می‌شود؛ عنوان/تامبنیل/لینک آپارات از پنل ادمین (`manufacturerHeroVideo*`)
- تا ثبت لینک: تامبنیل fallback (تصویر خود اسلاید ← گرادیان) + نشان کهربایی «به‌زودی»؛ کلیک بدون لینک → اطلاع‌رسانی Toast؛ پس از ثبت لینک، مودال پخش آپارات فعال است
- ریسپانسیو موبایل/دسکتاپ تأیید شد (اسکرین‌شات QA)

### تغییر ۱۱ — Breadcrumb در همهٔ صفحات اصلی
- افزوده‌شده به: درباره/تماس (۳ حالت)، برای معماران، برای تولیدکنندگان، آموزش، معرفی، همکاری مدل‌سازان، صفحهٔ برند (+ دارک‌مود تأیید شد)
- برچسب‌ها هماهنگ با منوی اصلی در FA/EN («برای معماران و متخصصان BIM»، «تماس و پشتیبانی» و...)

### تغییر ۱۲ (فنی) — رفع باگ رفرش ناخواستهٔ کل صفحه هنگام ثبت داده
- ریشه: watcher محیط dev پس از نوشتن در `data/*.json` کل صفحه را reload می‌کرد و وضعیت React (مثل پیام موفقیت تیکت) پاک می‌شد
- `server.ts`: `createViteServer({ server: { watch: { ignored: ["**/data/**", "**/server.ts", ...] } } })` — تأیید با تست Playwright (صفر reload، ماندگاری پیام موفقیت + شماره پیگیری)

---

## نسخه ۱۴۰۵/۰۵/۱۶_۰۳ — فاز ۲: تجربه کاربری (UX)
**این نسخه = فایل `IranBIMhub_05.16_03.zip`**

### تغییر ۱ — سیستم اعلان Toast به‌جای alert مرورگر
- فایل جدید: `src/components/ui/toast.tsx`
- همهٔ **۱۰۵ فراخوان `alert()`** در ۹ فایل با `toast()` جایگزین شد: `App.tsx`، `AuthModal.tsx`، `ModelerDashboard.tsx`، `ManufacturerDashboard.tsx`، `LearnView.tsx`، `BrandPageView.tsx`، `BrandOwnershipVerificationPanel.tsx`، `BrandVerificationAdminView.tsx`، `AdminControlPanel.tsx`
- اعلان غیرمسدودکننده، راست‌چین/چپ‌چین خودکار، انیمیشن ورود/خروج، دکمه بستن، حداکثر ۴ اعلان هم‌زمان، نمایش بالای موبایل‌نوی‌بار
- **تشخیص خودکار نوع پیام** بر اساس کلیدواژه (خطا/موفقیت/هشدار/اطلاع) با امکان تعیین دستی: `toast(msg, 'success')` یا `toast.error(msg)`
- رنگ‌بندی از توکن‌های وضعیت تأییدشده در `index.css` (`--color-state-*`)؛ سازگار با دارک‌مود
- `ToastProvider` در ریشهٔ `App.tsx` نصب شد

### تغییر ۲ — کاروسل‌های صادق (حذف تکرار سه‌تایی آیتم‌ها)
- فایل: `src/components/views/HomeView.tsx`
- تا قبل، ردیف «نمونه‌آبجکت‌ها» ۴ کارت را **۱۲ بار (۳×۴)** و ردیف «برندها» ۳ کارت را **۹ بار (۳×۳)** نمایش می‌داد تا اسکرول بی‌نهایت بسازد — ظاهری مصنوعی و غیرواقعی
- حالا: اگر تعداد آیتم‌های واقعی **کمتر از ۶** باشد، فقط **یک ردیف واقعی** نمایش داده می‌شود؛ اسکرول خودکار متوقف، دکمه‌های کناری در دسکتاپ مخفی می‌شود
- وقتی کتابخانه واقعی به ۶+ آیتم برسد، مارکی بی‌نهایت **خودکار فعال می‌شود** (بدون نیاز به تغییر کد)

### تغییر ۳ — کامپوننت واحد EmptyState برای حالت‌های خالی
- فایل جدید: `src/components/ui/EmptyState.tsx`
- جایگزینی باکس‌های یک‌خطی ساده با کارت استاندارد (آیکون + عنوان + راهنما + اکشن اختیاری) در ۱۰ نقطه:
  - `ModelerDashboard`: دانلودهای اخیر، تاریخچهٔ دانلود (فیلتر)، پوشهٔ پروژهٔ خالی، لیست علاقه‌مندی‌ها
  - `ManufacturerDashboard`: صفحهٔ انتخاب لید/گفت‌وگو، نبود درخواست مطابق فیلتر، صفحهٔ جزئیات درخواست
  - `LearnView`: نبود مقالهٔ منطبق + اکشن پاک‌سازی فیلترها
  - `BrandPageView`: نبود آبجکت فعال + اکشن بازنشانی فیلترها

### تغییر ۴ — اصلاح متن بازاریابی LearnView (مانده از فاز ۱)
- ادعای ساختگی «برندها سهم بازار را تا ۲ برابر کرده‌اند» با متن واقعی و وقار تجاری جایگزین شد (بدون آمار غیرقابل اثبات)

### موارد گزارش‌شده — بدون تغییر، در انتظار تأیید مالک محصول
طبق دستور «بدون اجازه حذف نکن»، این موارد دست‌نخورده باقی ماندند:
1. `ModelerDashboard.tsx` (~خط ۱۵۷): دو پوشهٔ پروژهٔ پیش‌فرض ساختگی («هتل مسکونی شیراز»، «بیمارستان کرج») برای کاربر تازه‌ثبت‌نام
2. `ModelerDashboard.tsx` (~خط ۱۸۴): دو کلکسیون پیش‌فرض ساختگی
3. `ModelerDashboard.tsx` (~خط ۱۳۲): پیش‌فرض «۳ دانلود باقی‌مانده از ۵» برای کاربری که هرگز دانلود نکرده
4. `HomeView.tsx` (~خط ۴۲۲ تا ۵۵۳): حدود ۱۱۰ خط هندلر مردهٔ کاروسل «نظرات مشتریان» (بخشی که قبلاً حذف شده) — کد بی‌مصرف

---

## نسخه ۱۴۰۵/۰۵/۱۶_۰۲ — فاز ۱: پاک‌سازی اعتبار و صداقت
**این نسخه = فایل `IranBIMhub_05.16_02.zip`**
- حذف برندهای دموی واقع‌نما (آلوپن، بوتان، شیشه کاوه و...)
- حذف نظرات مشتریان ساختگی و آمارهای جعلی (۴۲۵۰ آبجکت، ۴۰٪، ۳۵هزار دانلود و...)
- خنثی‌سازی پیش‌فرض‌های داشبوردها و چک‌لیست‌های ارزیابی
- تصاویر محلی آبجکت‌ها به‌جای لینک‌های شکسته Unsplash
- حذف اسکریپت‌های موقت توسعه از ریشه
- جزئیات کامل: `IRANBIMHUB-AUDIT-PHASE1.md`
