# اطلس قابلیت‌های محصول ایران‌بیم‌هاب

این سند از حذف‌شدن ایده‌های ارزشمند در زمان ساده‌سازی نسخهٔ عمومی جلوگیری می‌کند. هر قابلیت ممکن است در کد وجود داشته باشد، اما تا آماده‌شدن داده و بک‌اند واقعی، از تجربهٔ عمومی پنهان بماند.

## قواعد وضعیت

- **عمومی در MVP:** در نمایشگاه و نسخهٔ عمومی قابل نمایش است.
- **داخلی / پیش‌نمایش:** کد فرانت نگه داشته می‌شود، اما نمایش عمومی ندارد.
- **فاز بعد:** وابسته به بک‌اند، دیتابیس، امنیت یا دادهٔ واقعی است.

| قابلیت | کامپوننت‌های اصلی | وضعیت | شرط فعال‌سازی عمومی |
|---|---|---|---|
| دایرکتوری برندها و ساختار پروفایل | `AboutView`، `BrandPageView` | عمومی در MVP | استفاده از پروفایل نمونه یا برند رسمیِ منتشرشده |
| دیدگاه متخصصان BIM | `HomeView`، `AdminControlPanel`، `SiteConfigContext` | عمومی در MVP | رضایت صاحب دیدگاه و ثبت/انتشار از پنل ادمین |
| آمار بازدید و دانلود | `ObjectDetailView`، `ManufacturerDashboard` | داخلی / پیش‌نمایش | API تحلیلی، دادهٔ واقعی و تعریف حریم خصوصی |
| رتبه‌بندی و امتیاز آبجکت | کارت و صفحهٔ محصول | داخلی / پیش‌نمایش | سیاست ضدتقلب، کاربر واقعی و دادهٔ کافی |
| درخواست محصول یا تکمیل اطلاعات | `ObjectDetailView` | عمومی در MVP | اتصال فرم به API/CRM؛ فعلاً ساختار فرانت موجود است |
| بررسی مقدماتی فنی آبجکت | پنل ادمین و پنل تولیدکننده | داخلی / پیش‌نمایش | تعریف چک‌لیست، ارزیاب، پرداخت دستی و گزارش بررسی |
| داشبورد «مسیر شروع برند» | `ManufacturerDashboard` | عمومی پس از ورود واقعی | حساب کاربری و پروفایل برند واقعی |
| «فضای حرفه‌ای برند» | `ManufacturerDashboard` | داخلی / پیش‌نمایش | دادهٔ محصول، نسخه‌بندی فایل، مجوز نقش و API |
| میز کار روزمرهٔ طراح | `ModelerDashboard` | عمومی پس از ورود واقعی | OTP و تاریخچه/علاقه‌مندی واقعی |
| فضای حرفه‌ای طراح | `ModelerDashboard` | داخلی / پیش‌نمایش | پروژه‌ها، کالکشن‌ها، فایل‌های واقعی و سیاست اشتراک |
| پرداخت و صورتحساب | `PaymentView`، داشبوردها | فاز بعد | خدمت تعریف‌شده، پیش‌فاکتور، فرآیند مالی و درگاه در صورت نیاز |
| تحلیل برند و سرنخ | `ManufacturerAnalyticsView` | فاز بعد | دادهٔ واقعی، رضایت کاربر، شاخص‌های معتبر و API امن |
| انتشار عمومی برند | `BrandOwnershipVerificationPanel`، `BrandPageView` | فاز بعد | احراز رابطه با برند + وضعیت انتشار عمومی |

## معماری تصمیم‌گیری

- یک دادهٔ مرکزی؛ دو سطح تجربه: ساده و حرفه‌ای.
- آمار و قابلیت‌های حساس ابتدا در پنل داخلی یا خصوصی، نه در تجربهٔ عمومی.
- صفحهٔ برند عمومی فقط برای برند رسمیِ منتشرشده یا پروفایل نمونهٔ برچسب‌خورده.
- هیچ قابلیت آینده به‌خاطر پنهان‌شدن از رابط عمومی حذف نمی‌شود؛ در این سند و کد نگه‌داری می‌شود.
