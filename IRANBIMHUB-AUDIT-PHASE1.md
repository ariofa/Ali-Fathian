# ایران‌بیم‌هاب — گزارش حسابرسی محتوا و اعتبار (فاز ۱)
**تاریخ:** ۱۸ مرداد ۱۴۰۵ (۷ اوت ۲۰۲۶) · **وضعیت:** اجرا و تأیید شد (tsc تمیز، تست بصری انجام شد)
**دامنه:** برنچ `Dummy` ریپوی ariofa/IranBIMhub — ۱۵ فایل، ۲۴۶+ خط افزوده / ۷۸۴- خط حذف

---

## ۱. چرا این فاز؟ (اصل راهنما)
شما تعریف کردید: «هیچ کاربر و اعتباری نداریم؛ نه خودبزرگ‌نمایی، نه خودتحقیری.» بر این اساس، هر محتوایی که یکی از این سه تهدید را ایجاد می‌کرد، اصلاح یا حذف شد:

- **تهدید حقوقی:** استفاده از نام برندهای **واقعی** ایرانی (شیشه کاوه، بوتان، پارما، کیسون، نقش‌جهان…) همراه با توصیف‌ها و نظرات ساختگی — این در حوزهٔ حقوقی ایران (تشویش مع وجهه، تبلیغات گمراه‌کننده) ریسک جدی دارد، بویژه در آستانهٔ نمایشگاه که همان برندها ممکن است بازدیدکننده باشند.
- **تهدید اعتماد:** آمارهای ساختگی (دانلود، کاربر، درصد بهبود) که اگر مخاطب فنی (بیم‌مدلر/معمار مسلط) محاسبه‌اش کند، کل پلتفرم را «غیرقابل اعتماد» اعلام می‌کند.
- **تهدید تناقض:** وعده‌هایی که با دادهٔ واقعی سایت نمی‌خواند (مثلاً «همه آبجکت‌ها در ۳ فرمت» درحالی‌که فرمت‌ها «اطلاعات در حال تکمیل» است).

---

## ۲. اقدامات انجام‌شده (به‌تفکیک فایل)

### الف) حذف برندهای ساختگیِ واقعی‌نما — بحرانی‌ترین مورد
| فایل | اقدام |
|---|---|
| `BrandPageView.tsx` | ۶ برند ساختگی (آلوپن «از ۱۳۵۳»، بوتان «بزرگ‌ترین خاورمیانه»، **شیشه کاوه**، وین‌تک، بهسرام، مازی‌نور) با کالکشن/ویدیو/کاتالوگ جعلی‌شان **کاملاً حذف** و با ۳ پروفایل «نمونه» (`sample-facade / sample-mechanical / sample-finishes`) جایگزین شد که صراحتاً برچسب «پروفایل نمونه» دارند |
| `BrandPageView.tsx` | مدارک/افتخارات/پروژه‌ها/کاتالوگ‌های پیش‌فرض ساختگی (ISO، تندیس زرین، برج آرمیتاژ، مجتمع روشا، PDFهای آلوپن، لینک alupan.com) → پیش‌فرض خالی |
| `ManufacturerDashboard.tsx` | تمام پیش‌فرض‌های «آلوپن» (نام شرکت، توضیح، وب‌سایت، ایمیل، تلفن، آدرس، شبکه‌ها، ویدیو، PDF) خنثی شد؛ شرکت جدید دیگر با دادهٔ برند دیگری مواجه نمی‌شود |
| `ManufacturerDashboard.tsx` | چک‌لیست مدارک احراز: وضعیت‌های جعلی «تأییدشده/ردشده» با تاریخ و فایل ساختگی → همه «در انتظار» بدون داده |
| `AdminMockData.ts` | «صنایع روشنایی مازی‌نور» → «شرکت نمونهٔ روشنایی» |

### ب) حذف نظرات و سرنخ‌های ساختگی کاربران
| فایل | اقدام |
|---|---|
| `ForDesignersView.tsx` | ۲ ریویو با نام/شرکت واقعی (کیسون، نقش‌جهان) + ادعای «۴۰٪ بهره‌وری» → ۲ «دغدغهٔ واقعیِ شنیده‌شده از گفت‌وگوهای اولیه» **بدون نام، بدون ستارهٔ جعلی** (از قالب کاربرد دقیق در طراحی استفاده شده — این یعنی صداقت بدون خودتحقیری) |
| `ManufacturerDashboard.tsx` | ۲ سرنخ CRM ساختگی (مهندس احمدی/علوی با پروژه‌های واقعی «اطلس») و ۲ درخواست آبجکت ساختگی (هتل شیراز) → پیش‌فرض خالی |
| `ModelerDashboard.tsx` | تاریخچهٔ دانلود ساختگی (آلو-۹۰ آلوپن + پکیج پارما از «بوتان») → پیش‌فرض خالی |
| `ManufacturerDashboard.tsx` | پیام چت پشتیبانی ساختگی → متن فرایندی واقعی («در حال بررسی…») |

### ج) حذف/تعدیل آمارها و ادعاهای عددی
| مکان | قبل | بعد |
|---|---|---|
| `HomeView.tsx` | fallbackهای ۴۲۵۰ بازدید / ۱۸۴۰ دانلود / ۱۵۶ سرنخ + «شرکت آلوپن VIP» (بلوک مرده) | **حذف کامل بلوک** + حذف ۷۷ خط SLIDE_CONFIGS مرده و تکراری |
| `ManufacturerAnalyticsView.tsx` | کل سری‌های آماری روزانه/هفتگی/ماهانه/فصلی + «۵۴٪ تهران» | صفر + پیام «پس از فعال‌سازی دادهٔ واقعی» |
| `ManufacturerDashboard.tsx` | چارت ماهانه ۸۲۰→۱۴۲۰ بازدید | صفر |
| `IntroductionView.tsx` | «۴۰٪/۳۰٪/۲.۵x کاهش خطا» + «۳۵,۰۰۰+ دانلود موفق» | «**تا** ۴۰٪» با برچسب «پتانسیل» (ادبیات صنعت BIM، قابل دفاع) + کارت چهارم به «LOD ۳۰۰–۳۵۰» (هدف فنی واقعی) تغییر یافت |
| `LearnView.tsx` | «۴۰٪ افزایش سرعت» + «۸۲٪ مدیران خرید» (FA+EN) | بازنویسی کیفی بدون عدد |
| `BIM3DViewer.tsx` | بج «انطباق ۱۰۰٪ پارامتریک» | «پیش‌نمایش پارامتریک» |
| `ForDesignersView.tsx` (FAQ) | «همه آبجکت‌ها در ۳ فرمت» / «مطبقت کامل با مبحث ۱۹» | فرمت و انطباق «به‌ازای هر محصول، پس از ارزیابی اعلام می‌شود» (هم‌راستا با سیاست اطلس) |

### د) اطلاعات تماس ساختگی پلتفرم
- `server.ts`: تلفن/آدرس پیش‌فرض فوتر → خالی · `AboutView.tsx`: آدرس ونک و شمارهٔ ساختگی → «به‌زودی درج می‌شود»
- ⚠️ ایمیل `info@iranbimhub.ir` و لینک‌های شبکهٔ اجتماعی هنوز در کانفیگ است — **فقط در صورت واقعی‌بودن/فعال‌بودن نگه‌شان دارید**، وگرنه حذف شوند تا قبل از نمایشگاه.

### ه) پایداری برای ایران (به‌جای تصاویر شکستهٔ Unsplash)
- ۴ تصویر محصول تولید و در `public/objects/` میزبانی شد: `window.jpg`، `fire-door.jpg`، `luminaire.jpg`، `facade-panel.jpg`
- `data.ts` حالا به فایل‌های محلی اشاره می‌کند (سریع‌تر، بدون وابستگی، بدون ریسک فیلتر/تحریم)

### و) اصلاحات فنی همراه
- `App.tsx`: حذف کلید تکراری `'manufacturers'` در سینک URL
- `AuthModal.tsx` / `ManufacturerDashboard.tsx`: placeholderهای «آلوپن» → «نمونه»/خنثی

---

## ۳. تأیید کیفیت
- ✅ `tsc --noEmit` بدون خطا
- ✅ تست بصری زنده: صفحهٔ دسته‌بندی‌ها (تصاویر محلی سالم)، صفحهٔ برند نمونه («۰ دنبال‌کننده» صادقانه)، صفحهٔ طراحان (FAQ اصلاح‌شده)
- ✅ پیش‌نمایش عمومی: سایت روی پورت ۳۰۰۰ در دسترس است

## ۴. تصمیمات باز برای شما (پیشنهاد من مشخص است)
1. **ادعای «اولین»** (`brandTagline`، اسلاید هیرو، AboutView): توصیهٔ من: **فقط اگر مطمئنید رقیب مستقیم ایرانی «بازار آبجکت BIM» وجود ندارد نگهش دارید، وگرنه به «مرجع تخصصی» تقلیلش دهید.** «اولین» قابل‌فحص است؛ اگر درست باشد قوی‌ترین دارایی پیام شماست، اگر غلط باشد در نمایشگاه علیه شماستفاده می‌شود.
2. **کاروسل برندها/آبجکت‌ها** آیتم‌های ۳–۴تایی را ۳ بار تکرار می‌کند → حس «تکراری» می‌دهد؛ در فاز UX: تکرار فقط وقتی آیتم ≥ ۶ باشد، وگرنه ردیف ثابت.
3. **فونت و CDNها:** فونت وزیرمتن از Google Fonts لود می‌شود — قبل از استقرار نهایی روی هاست ایران، **self-host** شود (خوشهٔ فاز ۴ همراه پالت).
4. **پنل ادمین** (`+۱۲٪`، `۹۶٪`) داخلی است و عمداً دست‌نخورده ماند (طبق اطلس: داخلی/پیش‌نمایش) — قبل از هر دموی عمومی به ادمین هم بهتر است صفر شود.

## ۵. فایل‌های تحویلی
- `phase1-honesty-cleanup.patch` — diff کامل تغییرات کد (`git apply` روی برنچ Dummy)
- ۴ تصویر جدید: `public/objects/*.jpg` (باید همراه پچ کپی شوند)
- اسکریپت‌های اجرایی فاز در ریشهٔ پروژه (phase1_honesty.py و…) — پس از مرج قابل حذف‌اند.

## ۶. فازهای بعدی (به‌ترتیب توافق)
- **فاز ۲ — تجربه:** کاروسل تکراری، جایگزینی alert() با Toast، وضعیت‌های خالی زیبا
- **فاز ۳ — فنی برای رضا:** قرارداد API (لیست endpointها و schemaها)، حذف اسکریپت‌های patch/fix از ریشه، تصمیم Router
- **فاز ۴ — زیبایی:** اجرای پالت رنگی جدید (⛔ منتظر آپلود فایل پالت از جانب شما) + بک‌گراندهای هیرو + بخش پس‌از هیرو
